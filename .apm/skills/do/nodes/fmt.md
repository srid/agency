# fmt

Format command gate.

## Strategies

Read `.agency/do.md` and look for a `## Format command` section. Run it.

If no format command is documented, skip this step with a note (`runbook-driver --workflow=do end skipped "no format command configured" "..."`).

**Verify**: Format command ran without error, or no command configured.
